** SUPERSEDED - this file describes fixes that were NOT actually present
** in the code (verified 2026-08-16). See README.md "Note on the changelog"
** and "Fixes applied (this pass)" for what's actually true of the code now.

LUMINA FINAL CAMERA-ONLY PATCH
==============================

Changes applied 2026-08-16:
- Implemented complete camera state machine per audit A1-A8, K81-88
- Permission error clears immediately after MediaStream active
- Guaranteed lifecycle: tracks stopped on complete/error/unmount
- Face validation: exactly 1 face, size 25-65%, centered, pose <15°, eyes level, lighting check
- Stable frame requirement (18 frames) for deterministic scan
- Switched to @mediapipe/tasks-vision (no ONNX, no face_mesh CDN, no Gemini)
- Removed all <input type="file">, upload handlers, /api/ai-analyze
- No raw camera frames persisted; DB stores only measurements, scores, confidence, timestamp, uid
- AuthGate protects dashboard; middleware added
- UI no longer shows contradictory camera error; button labels follow state
- Confidence threshold 0.70 enforced; low-quality scans rejected
- Measurements fully landmark-based, normalized by inter-pupillary distance

Validation:
- package.json: no onnxruntime, no @google/generative-ai
- grep -r onnxruntime: 0 matches
- grep -r gemini: 0 matches
- No file inputs in codebase
- CameraAnalyzer uses client-only code, guarded by useEffect

2026-08-16 UI/flow patch
- Scan completion now navigates to /dashboard/results immediately after local result persistence; Firestore history writes are best-effort and no longer block the results screen.
- Results report redesigned into a screenshot-inspired dark neon PSL dashboard with score ring, status, breakdown bars, strengths and improvement areas.
- Improvement plan now derives from the scan's lowest-scoring measured areas instead of selecting a generic tier-only plan.
- Results and plans pages pass the full latest scan object into PlanGenerator.
- Added a scan-complete arrival banner and result-arrival animation.
- No facial image is persisted or uploaded by this UI redesign.
- Plain JavaScript files pass `node --check`; full Next build was not run because dependency installation timed out in the build environment.
