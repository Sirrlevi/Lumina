"use client";import {createUserWithEmailAndPassword} from "firebase/auth";import {auth} from "@/lib/firebase";import {useState} from "react";import {useRouter} from "next/navigation";import B from "@/components/GoogleButton";export default function R(){const[f,sf]=useState({});const r=useRouter();return <form onSubmit={async e=>{e.preventDefault();await createUserWithEmailAndPassword(auth,f.email,f.pass);r.push("/dashboard/upload")}} className="glass p-6 max-w-md mx-auto mt-10 space-y-3"><input placeholder="Email" onChange={e=>sf({...f,email:e.target.value})} className="w-full p-2 bg-black/30 rounded"/><input type="password" placeholder="Pass" onChange={e=>sf({...f,pass:e.target.value})} className="w-full p-2 bg-black/30 rounded"/><button className="btn-neon w-full">Sign Up</button><B text="Sign up with Google"/></form>}