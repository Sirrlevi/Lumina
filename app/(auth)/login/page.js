"use client";import {signInWithEmailAndPassword} from "firebase/auth";import {auth} from "@/lib/firebase";import {useState} from "react";import {useRouter} from "next/navigation";import B from "@/components/GoogleButton";export default function L(){const[e,se]=useState(""),[p,sp]=useState("");const r=useRouter();return <form onSubmit={async ev=>{ev.preventDefault();await signInWithEmailAndPassword(auth,e,p);r.push("/dashboard/upload")}} className="glass p-6 max-w-md mx-auto mt-10 space-y-3"><input placeholder="Email" onChange={e=>se(e.target.value)} className="w-full p-2 bg-black/30 rounded"/><input type="password" placeholder="Pass" onChange={e=>sp(e.target.value)} className="w-full p-2 bg-black/30 rounded"/><button className="btn-neon w-full">Login</button><B/></form>}